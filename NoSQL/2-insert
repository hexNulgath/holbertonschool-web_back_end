// Insert the document and simulate the output
const result = db.school.insertOne({
  name: "Holberton school"
});

// Mimic the legacy mongo shell output format
printjson(WriteResult({ "nInserted": result.acknowledged ? 1 : 0 }));
